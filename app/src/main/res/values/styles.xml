<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <!-- Violet application theme. -->
    <style name="VioleTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/VioletPrimary</item>
        <item name="colorPrimaryDark">@color/VioletPrimaryDark</item>
        <item name="colorAccent">@color/VioletPrimaryLight</item>
    </style>

</resources>
